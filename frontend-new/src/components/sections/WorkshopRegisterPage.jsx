import React, { useState } from 'react';
import { Camera, Check, ChevronLeft, Drumstick, Leaf, MessageCircle, QrCode } from 'lucide-react';
import { Button } from '../ui/Button';

const whatsappCommunityUrl = import.meta.env.VITE_WHATSAPP_COMMUNITY_URL || 'https://www.whatsapp.com/';

export function WorkshopRegisterPage({ setPage }) {
  const [form, setForm] = useState({ name: '', college: '', email: '', phone: '', food: 'veg', screenshot: '' });
  const [step, setStep] = useState('details');
  const [errors, setErrors] = useState({});
  const edit = (key, value) => { setForm((current) => ({ ...current, [key]: value })); setErrors({}); };

  const openPayment = () => {
    const nextErrors = {};
    if (!form.name.trim()) nextErrors.name = 'Enter your name.';
    if (!form.college.trim()) nextErrors.college = 'Enter your college name.';
    if (!/^\S+@\S+\.\S+$/.test(form.email)) nextErrors.email = 'Enter a valid email address.';
    if (!/^\d{10}$/.test(form.phone)) nextErrors.phone = 'Enter a valid 10-digit mobile number.';
    setErrors(nextErrors);
    if (!Object.keys(nextErrors).length) setStep('payment');
  };

  if (step === 'done') return <main className="workshop-registration-page"><section className="workshop-confirmation"><div className="workshop-confirm-icon"><Check size={30} /></div><p className="eyebrow">WORKSHOP REGISTRATION</p><h1>Payment details<br /><span>submitted.</span></h1><p>Your workshop registration is ready for review. Join the WhatsApp community for updates.</p><div className="workshop-confirm-id"><span>Workshop fee</span><strong>₹250</strong><small>Data Science with AI Technology</small></div><a className="whatsapp-community" href={whatsappCommunityUrl} target="_blank" rel="noopener noreferrer"><MessageCircle size={18} /> Join the WhatsApp community</a><Button className="confirmation-home" onClick={() => setPage('home')}>Back to home</Button></section></main>;

  return <main className="workshop-registration-page"><section className="workshop-registration-shell"><button className="workshop-back" type="button" onClick={() => setPage('home')}><ChevronLeft size={16} /> Back to FENIX&apos;26</button><div className="workshop-registration-intro"><p className="eyebrow">FEATURED HANDS-ON WORKSHOP</p><h1>Data Science with<br /><span>AI Technology.</span></h1><p>Reserve your individual workshop seat and complete the ₹250 GPay payment.</p><div className="workshop-registration-meta"><span>Individual registration</span><b>₹250</b><span>Food included</span></div></div><section className="workshop-form-card"><div className="workshop-form-progress"><span className={step === 'details' ? 'active' : ''}>01 <b>Your details</b></span><i /><span className={step === 'payment' ? 'active' : ''}>02 <b>Payment</b></span></div>{step === 'details' ? <div className="workshop-form-fields"><h2>Reserve your seat</h2><p>Fill in your details to view the GPay payment QR.</p><label>Full name<input value={form.name} onChange={(event) => edit('name', event.target.value)} placeholder="Your full name" /></label>{errors.name && <small className="error">{errors.name}</small>}<label>College name<input value={form.college} onChange={(event) => edit('college', event.target.value)} placeholder="Your college name" /></label>{errors.college && <small className="error">{errors.college}</small>}<div className="workshop-field-pair"><label>Email address<input type="email" value={form.email} onChange={(event) => edit('email', event.target.value)} placeholder="you@college.edu" /></label><label>Mobile number<input inputMode="numeric" value={form.phone} onChange={(event) => edit('phone', event.target.value.replace(/\D/g, '').slice(0, 10))} placeholder="10-digit number" /></label></div>{(errors.email || errors.phone) && <small className="error">{errors.email || errors.phone}</small>}<fieldset><legend>Food preference</legend><div className="workshop-food-options"><label className={form.food === 'veg' ? 'selected' : ''}><input type="radio" name="workshop-food" checked={form.food === 'veg'} onChange={() => edit('food', 'veg')} /><Leaf size={18} /><span><b>Vegetarian</b><small>Veg meal</small></span></label><label className={form.food === 'non-veg' ? 'selected' : ''}><input type="radio" name="workshop-food" checked={form.food === 'non-veg'} onChange={() => edit('food', 'non-veg')} /><Drumstick size={18} /><span><b>Non-vegetarian</b><small>Non-veg meal</small></span></label></div></fieldset><Button onClick={openPayment}>View GPay QR · ₹250</Button></div> : <div className="workshop-payment-step"><h2>Complete your payment</h2><p>Scan the GPay QR and upload the payment screenshot.</p><div className="workshop-payment-layout"><div className="gpay-qr-card"><div className="gpay-mark"><span>G</span> Pay</div><div className="gpay-qr"><QrCode size={150} strokeWidth={1.8} /></div><strong>₹250</strong><small>Workshop registration fee</small></div><div className="workshop-upload-copy"><p className="workshop-payment-note">After making the payment, add the screenshot below.</p><input id="workshop-screenshot" type="file" accept="image/png,image/jpeg,image/webp" onChange={(event) => edit('screenshot', event.target.files?.[0] || '')} /><label className="workshop-upload" htmlFor="workshop-screenshot"><Camera size={19} /><span>{form.screenshot ? form.screenshot.name : 'Upload payment screenshot'}</span><small>PNG, JPG or WEBP</small></label>{errors.screenshot && <small className="error">{errors.screenshot}</small>}</div></div><div className="workshop-payment-actions"><Button secondary onClick={() => setStep('details')}>Edit details</Button><Button onClick={() => form.screenshot ? setStep('done') : setErrors({ screenshot: 'Upload the payment screenshot before submitting.' })}>Submit registration <Check size={15} /></Button></div></div>}</section></section></main>;
}
